d1 <- 1:50
d2 <- 51:100

d3 <- c(d1[1:10], d2[1:10])
d3

# sort(d1, decreasing = TRUE)
# 내림차순 정렬

# mean(d1)   # 벡터에 포함된 값들의 평균
# mean(d2)
# mean(d2) - mean(d1)   # 두 평균의 차이

# max(d2)   # 벡터에 포함된 값들의 최댓값
# min(d2)   # 벡터에 포함된 값들의 최솟값

# sum(d1, d2)

# d1
# d2

# length(d2)

# d1 + d2
# d2 - d1
# d1 * d2
# d2 / d1

# sum(d1)   # sum함수: 벡터에 포함된 값들의 합
# sum(d2)